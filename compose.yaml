services:
  prometheus:
    command:
      - --config.file=/etc/prometheus/config.yaml
      - --web.enable-remote-write-receiver
      - --log.level=debug
    image: prom/prometheus:v3.1.0
    ports:
      - 9090:9090
